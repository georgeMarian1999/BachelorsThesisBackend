import { Subjects } from "./subjects";
export interface ProfesorUpdatedEvent {
    subject: Subjects.ProfesorUpdated;
    data: {
        profesor_id: string;
        facultate_id: string;
        numar_locuri: number;
    };
}
